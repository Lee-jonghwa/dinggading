AddInstrumentRequest:
  type: object
  properties:
    instrument:
      type: string
      enum:
        - VOCAL
        - GUITAR
        - DRUM
        - BASS
      description: 악기 타입
    tier:
      $ref: "../schemas/tier.yaml#/Tier"
  required:
    - instrument
