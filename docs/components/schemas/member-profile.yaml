
MemberProfile:
  type: object
  properties:
    member:
      $ref: './member.yaml#/Member'
    instrumentTiers:
      type: array
      items:
        $ref: './member-instrument-tier.yaml#/MemberInstrumentTier'
      description: 회원의 악기별 티어 정보
    favoriteBand:
      $ref: './band.yaml#/Band'

    isFollowing:
      type: boolean
      description: 현재 로그인한 사용자가 이 회원을 팔로우하고 있는지 여부
    isFollowed:
      type: boolean
      description: 이 회원이 현재 로그인한 사용자를 팔로우하고 있는지 여부
    followerCount:
      type: integer
      description: 이 회원을 팔로우하는 사람 수
    followingCount:
      type: integer
      description: 이 회원이 팔로우하는 사람 수